[project]
name = "gh-copilot-sdk-repo-analyser"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "github-copilot-sdk>=0.3.0",
    "asyncio",
    "httpx",
    "pydantic",
    "fastapi",
    "uvicorn",
    "python-dotenv"
]